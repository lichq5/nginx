
default:	build

clean:
	rm -rf Makefile objs

build:
	$(MAKE) -f objs/Makefile

install:
	$(MAKE) -f objs/Makefile install

modules:
	$(MAKE) -f objs/Makefile modules

upgrade:
	/mnt/SDCARD/arm-nginx/nginx/sbin/nginx -t

	kill -USR2 `cat /mnt/SDCARD/arm-nginx/nginx/logs/nginx.pid`
	sleep 1
	test -f /mnt/SDCARD/arm-nginx/nginx/logs/nginx.pid.oldbin

	kill -QUIT `cat /mnt/SDCARD/arm-nginx/nginx/logs/nginx.pid.oldbin`
